# CiviClaw Taipei Daily News - Python dependencies
# Install with: pip install -r requirements.txt
# (also run `playwright install chromium` once after installing)

# Web rendering / screenshots
playwright

# Environment variable loading (.env)
python-dotenv

# Google auth + APIs (Vertex AI image gen via REST, YouTube upload)
google-auth
google-auth-oauthlib
google-api-python-client
